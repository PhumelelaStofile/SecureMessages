# SecureMessages
Secure Messages App is a desktop-based GUI application that provide secure message handling using the Caesar Encryption Technique (CET). The application allows users to open plain text files, encrypt messages, and securely store them in a database — all within an easy-to-use graphical interface.
